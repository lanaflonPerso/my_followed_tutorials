## ES6-Boilerplate

```bash

mkdir es6-boilerplate
cd es6-boilerplate
npm init -yes


yarn add -D webpack webpack-dev-server
yarn add -D babel-loader babel-core babel-preset-es2015

yarn add -D eslint eslint-watch npm-run-all
```
